
case class Person(name: String,
                  isMale: Boolean,
                  children: Person*)

object sc {
 val Decimal = """(-)?(\d+)(\.\d*)?""".r
val Decimal(sign, integerpart, decimalpart) = "1.23"
   def a = <a> {{brace yourself!}} </a>
}